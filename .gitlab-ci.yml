stages:
    - fmt
    - builds
    - tests
    
before_script:
    - export PATH="$HOME/.cargo/bin:$PATH"
    - which rustfmt || cargo install rustfmt-nightly
    
fmt:
  stage: fmt
  tags:
    - redshift-rs
  script:  
    - cargo fmt -- --write-mode=diff
    
builds:
  stage: builds
  tags:
    - redshift-rs
  script:
    - cargo build
    
tests:
  stage: tests
  tags:
    - redshift-rs
  script:
    - cargo test
