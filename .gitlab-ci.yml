stages:
    - builds
    - tests
    - fmt
    
before_script:
    - export PATH="$HOME/.cargo/bin:$PATH"
    
builds:
  stage: builds
  tags:
    - redshift-rs
  script:
    - cargo build
    
tests:
  stage: tests
  tags:
    - redshift-rs
  script:
    - cargo test
    
fmt:
  stage: fmt
  tags:
    - redshift-rs-nightly
  script:
    - which rustfmt || cargo install rustfmt-nightly
    - cargo fmt -- --write-mode=diff
