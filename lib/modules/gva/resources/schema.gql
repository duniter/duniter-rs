
schema {
  query: Query
  mutation: Mutation
}

type Query {
  current: Block @juniper(ownership: "owned")
}

type Mutation {
  noop: Boolean!
}

type Block {
  version: Int!
  currency: String!
  issuer: String!
  number: Int!
}
